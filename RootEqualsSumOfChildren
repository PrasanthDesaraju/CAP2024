class Solution {
    public boolean checkTree(TreeNode root) {
        int rootval = root.val;
        int left = root.left.val;
        int right = root.right.val;
        if ((left + right) == rootval) return true;
        return false;
    }
}
